/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['generally-eager-donkey.ngrok-free.app', 'physio-api.zerologicspace.com'],
},
};

export default nextConfig;
